```
用例名称： 获取历年分数线
前置条件： 
	pass
用例细节(正常流程)：
	1.用户在（院校-专业 页面）中点击 查看分数线功能图标，进入分数线页面模块
	2.系统获取到传递过来的 uid（院校id） 和 mid（专业id）数据
	3.系统判断该请求信息是否合法,如果合法则继续
	4.系统提取成绩线数据
	5.系统确定成绩线数据数量>=2
	6.系统将数据渲染到 二维折线图中
	7.系统返回分数线页面，用例结束。
例外(拓展流程)：
	5a：成绩线数据数量不满足>=2,且成绩线数据数量=0
		5a1：系统返回分数线页面
		5a2: 系统提示用户没有数据
		5a2: 系统返回上级页面,用例结束
	5b：成绩线数量不满足>=2,且成绩线数据数量=1
		6b: 系统将渲染单个数据到页面上
		7b：系统返回分数线页面，用例结束
后置条件:
	pass
异常情况：
	3e: 请求信息(uid + mid)非法
		3e1: 若该院校未开设该专业,返回请求失败异常,并携带信息
```

